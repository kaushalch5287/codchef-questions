#include<stdio.h>
int main()
{
    int x;
    float y,cal=0;
    scanf("%d %f",&x,&y);
        if(x%5==0 && (x + 0.5)<=y){
              cal = y - x - 0.5;
            printf("%.2f",cal);
        }
        else{
            
            printf("%.2f",y);
        }
     
  
    return 0;
}
