#include<stdio.h>
int main()
{
    int t,n,k;
    scanf("%d",&t);
    int x[t],y[t],z[t];
    for(int i=0;i<t;i++){
        scanf("%d %d",&x[i],&y[i]);
        z[i]=x[i]%y[i];
    }
    for(int i=0;i<t;i++){
      
   printf("%d\n",z[i]);
    }
    
    return 0;
}
